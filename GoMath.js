const pageNumber = (total, max, current) =>{
   
}

function PaginationButtons (totalPages, maxPageVisible = 1, currentPage = 1){
let pages = PaginationButtons (totalPages, maxPageVisible, currentPage);
let currentPageBtn = null;

const PaginationButtons = document.createElement('div');
pagination.className = 'pagination-buttons';

this.render = (container = document.body)=>{
    container.appendChild(PaginationButtonsContainer)
}

}